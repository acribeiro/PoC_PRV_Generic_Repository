@model IEnumerable<Providencia.Entidades.PRV_GRUPO>

@{
    ViewBag.Title = "Index";
}

<h2></h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRV_NOME_GRUPO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRV_NOME_GRUPO)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.PRV_ID_GRUPO }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.PRV_ID_GRUPO }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.PRV_ID_GRUPO })
        </td>
    </tr>
}

</table>
