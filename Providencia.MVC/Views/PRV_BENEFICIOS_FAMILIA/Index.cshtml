@model IEnumerable<Providencia.Entidades.PRV_BENEFICIOS_FAMILIA>

@{
    ViewBag.Title = "Index";
}

<h2></h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRV_BENEFICIOS.PRV_DESCRICAO_BENEFICIOS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRV_CONSTITUICAO_FAMILIAR.PRV_NOME_MEMBRO_FAMILIA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRV_BENEFICIOS.PRV_DESCRICAO_BENEFICIOS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRV_CONSTITUICAO_FAMILIAR.PRV_NOME_MEMBRO_FAMILIA)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.PRV_ID_BENEFICIOS_FAMILIA }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.PRV_ID_BENEFICIOS_FAMILIA }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.PRV_ID_BENEFICIOS_FAMILIA })
        </td>
    </tr>
}

</table>
