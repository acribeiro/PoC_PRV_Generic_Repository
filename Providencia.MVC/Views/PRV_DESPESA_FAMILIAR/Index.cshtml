@model IEnumerable<Providencia.Entidades.PRV_DESPESA_FAMILIAR>

@{
    ViewBag.Title = "Index";
}

<h2></h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRV_DESCRICAO_DESPESA_FAMILIAR)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRV_DESCRICAO_DESPESA_FAMILIAR)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.PRV_ID_DESPESA_FAMILIAR }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.PRV_ID_DESPESA_FAMILIAR }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.PRV_ID_DESPESA_FAMILIAR })
        </td>
    </tr>
}

</table>
