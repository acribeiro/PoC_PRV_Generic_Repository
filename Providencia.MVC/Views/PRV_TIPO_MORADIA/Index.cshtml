@model IEnumerable<Providencia.Entidades.PRV_TIPO_MORADIA>

@{
    ViewBag.Title = "Index";
}

<h2></h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRV_DESCRICAO_TIPO_MORADIA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRV_DESCRICAO_TIPO_MORADIA)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.PRV_ID_TIPO_MORADIA }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.PRV_ID_TIPO_MORADIA }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.PRV_ID_TIPO_MORADIA })
        </td>
    </tr>
}

</table>
