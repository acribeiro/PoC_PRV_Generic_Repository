@model IEnumerable<Providencia.Entidades.PRV_ESTADO_CIVIL>

@{
    ViewBag.Title = "Index";
}

<h2></h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRV_DESCRICAO_ESTADO_CIVIL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRV_DESCRICAO_ESTADO_CIVIL)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.PRV_ID_ESTADO_CIVIL }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.PRV_ID_ESTADO_CIVIL }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.PRV_ID_ESTADO_CIVIL })
        </td>
    </tr>
}

</table>
